# Руководство по ручной проверке требований к исключениям

## 📋 Требования задания

1. ✅ **Минимум 5 типов исключительных ситуаций**
2. ✅ **Перенаправление исключительных ситуаций**
3. ✅ **Минимум одна исключительная ситуация с оператором new**
4. ✅ **Исключительная ситуация в конструкторе с демонстрацией вызова конструкторов и деструкторов**
5. ✅ **Собственная функция завершения**
6. ✅ **Собственный (пользовательский) класс исключения**

---

## 🔍 ДЕТАЛЬНАЯ ПРОВЕРКА КАЖДОГО ТРЕБОВАНИЯ

### ✅ ТРЕБОВАНИЕ 1: Минимум 5 типов исключительных ситуаций

**Где проверять:** Пункт меню `10. Демонстрация исключений` → Демонстрация 5

**Шаги для проверки:**
1. Запустите программу
2. Выберите пункт меню `10`
3. Прокрутите до раздела "Демонстрация 5: Различные типы исключений"

**Ожидаемый результат:**
Вы должны увидеть 5 различных типов исключений:

```
--- Демонстрация 5: Различные типы исключений ---
Тип 1 - invalid_argument: Опыт не может быть отрицательным!
Тип 2 - runtime_error: Вагон с номером 999 не найден!
Тип 3 - invalid_argument (логическая ошибка): Количество требуемых сотрудников не может быть отрицательным!
Тип 4 - runtime_error (недостаточно персонала): Недостаточно проводников! Требуется: 10, доступно: 0
Тип 5 - runtime_error (дубликат): Сотрудник с именем Петр уже существует!
```

**✅ Критерий успеха:** Вы видите минимум 5 различных типов исключений с разными сообщениями.

---

### ✅ ТРЕБОВАНИЕ 2: Перенаправление исключительных ситуаций

**Где проверять:** Пункт меню `10. Демонстрация исключений` → Демонстрация 4

**Шаги для проверки:**
1. Запустите программу
2. Выберите пункт меню `10`
3. Прокрутите до раздела "Демонстрация 4: Перенаправление исключений"

**Ожидаемый результат:**
```
--- Демонстрация 4: Перенаправление исключений ---
Перехвачено: Исходное исключение
Перенаправление исключения...
Поймано перенаправленное исключение: Исходное исключение
```

**Как это работает:**
- Исключение генерируется в функции `demonstrateExceptionForwarding()`
- Перехватывается внутри функции
- Затем перенаправляется с помощью `throw;` (без параметров)
- Внешний блок catch ловит перенаправленное исключение

**✅ Критерий успеха:** Вы видите сообщение "Перехвачено", затем "Перенаправление исключения...", и наконец "Поймано перенаправленное исключение" с тем же сообщением.

---

### ✅ ТРЕБОВАНИЕ 3: Исключительная ситуация с оператором new

**Где проверять:** Пункт меню `10. Демонстрация исключений` → Демонстрация 3

**Шаги для проверки:**
1. Запустите программу
2. Выберите пункт меню `10`
3. Прокрутите до раздела "Демонстрация 3: Исключение с оператором new"

**Ожидаемый результат:**
```
--- Демонстрация 3: Исключение с оператором new ---
Попытка выделить память (симуляция нехватки памяти)...
Поймано исключение bad_alloc (нехватка памяти): std::bad_alloc
Оператор new не смог выделить память, объект не создан.
```

**Как это работает:**
- Функция `Train::createTrainWithException()` генерирует `std::bad_alloc`
- Это исключение, которое выбрасывает оператор `new` при нехватке памяти
- Исключение перехватывается и обрабатывается

**✅ Критерий успеха:** Вы видите сообщение о нехватке памяти и обработку `std::bad_alloc`.

**Дополнительная проверка в коде:**
Откройте файл `Train.cpp`, строка 45-47:
```cpp
Train* Train::createTrainWithException(int trainNumber, const std::string& route) {
    throw std::bad_alloc();  // Симуляция нехватки памяти
}
```

---

### ✅ ТРЕБОВАНИЕ 4: Исключительная ситуация в конструкторе с демонстрацией конструкторов и деструкторов

**Где проверять:** Пункт меню `10. Демонстрация исключений` → Демонстрация 1

**Шаги для проверки:**
1. Запустите программу
2. Выберите пункт меню `10`
3. Прокрутите до раздела "Демонстрация 1: Исключение в конструкторе"

**Ожидаемый результат:**
```
--- Демонстрация 1: Исключение в конструкторе ---
Попытка создать Employee с пустым именем...
Поймано исключение EmployeeException: Имя сотрудника не может быть пустым!
ВАЖНО: При исключении в конструкторе деструктор НЕ вызывается!
Объект не был полностью создан, поэтому его не нужно удалять.

Для сравнения - успешное создание объекта:
Конструктор Employee: Иван
Конструктор Conductor: Иван
Объект успешно создан
Деструктор Conductor: Иван
Деструктор Employee: Иван
Объект удален (деструктор вызван)
```

**Что проверить:**
1. **При исключении в конструкторе:**
   - ❌ НЕ должно быть сообщений "Конструктор Employee" или "Конструктор Conductor"
   - ❌ НЕ должно быть сообщений "Деструктор"
   - ✅ Должно быть только сообщение об исключении

2. **При успешном создании (для сравнения):**
   - ✅ Должны быть сообщения "Конструктор Employee" и "Конструктор Conductor"
   - ✅ При удалении должны быть сообщения "Деструктор Conductor" и "Деструктор Employee"

**✅ Критерий успеха:** 
- При исключении в конструкторе деструктор НЕ вызывается
- При успешном создании вызываются и конструктор, и деструктор

**Дополнительная проверка в коде:**
Откройте файл `Employee.cpp`, строки 5-18:
```cpp
Employee::Employee(const std::string& name, int experience, double salary) {
    if (name.empty()) {
        throw EmployeeException("Имя сотрудника не может быть пустым!");
    }
    // ... остальной код конструктора
}
```

---

### ✅ ТРЕБОВАНИЕ 5: Собственная функция завершения

**Где проверять:** Функция `customTerminate()` вызывается при необработанном исключении

**Шаги для проверки:**
1. Откройте файл `main.cpp`
2. Найдите строку 239: `std::set_terminate(customTerminate);`
3. Убедитесь, что функция `customTerminate()` определена (строки 19-23)

**Проверка в коде:**
```cpp
// Строка 19-23
void customTerminate() {
    std::cerr << "\n=== ВЫЗВАНА СОБСТВЕННАЯ ФУНКЦИЯ ЗАВЕРШЕНИЯ ===" << std::endl;
    std::cerr << "Произошла необработанная исключительная ситуация!" << std::endl;
    std::abort();
}

// Строка 239
std::set_terminate(customTerminate);
```

**Как проверить работу функции:**
Чтобы увидеть функцию завершения в действии, нужно создать необработанное исключение. Это можно сделать, временно закомментировав обработку исключений.

**⚠️ ВНИМАНИЕ:** Это приведет к завершению программы!

**Тест (опционально, для проверки):**
1. Временно закомментируйте блоки `try-catch` в функции `demonstrateExceptions()`
2. Запустите программу
3. Выберите пункт меню `10`
4. Должно появиться сообщение:
```
=== ВЫЗВАНА СОБСТВЕННАЯ ФУНКЦИЯ ЗАВЕРШЕНИЯ ===
Произошла необработанная исключительная ситуация!
```

**✅ Критерий успеха:** 
- Функция `customTerminate()` определена
- Функция установлена через `std::set_terminate(customTerminate)`
- Функция выводит понятное сообщение об ошибке

---

### ✅ ТРЕБОВАНИЕ 6: Собственный (пользовательский) класс исключения

**Где проверять:** 
- В коде: `Employee.h` (определение класса)
- В действии: Пункт меню `10. Демонстрация исключений` → Демонстрация 2
- В действии: Пункт меню `3` или `4` с пустым именем

**Шаги для проверки в коде:**
1. Откройте файл `Employee.h`
2. Найдите определение класса (строки 8-11):
```cpp
class EmployeeException : public std::runtime_error {
public:
    EmployeeException(const std::string& message) : std::runtime_error(message) {}
};
```

**Шаги для проверки в действии (вариант 1 - демонстрация):**
1. Запустите программу
2. Выберите пункт меню `10`
3. Прокрутите до раздела "Демонстрация 2: Пользовательский класс исключения"

**Ожидаемый результат:**
```
--- Демонстрация 2: Пользовательский класс исключения ---
Обработано пользовательское исключение: Это пользовательское исключение EmployeeException!
```

**Шаги для проверки в действии (вариант 2 - через меню):**
1. Запустите программу
2. Выберите пункт меню `3. Добавить проводника`
3. Введите пустое имя (просто нажмите Enter)
4. Введите любой опыт (например, `5`)
5. Введите любую зарплату (например, `50000`)
6. Введите любой номер поезда (например, `101`)

**Ожидаемый результат:**
```
Ошибка EmployeeException: Имя сотрудника не может быть пустым!
```

**Дополнительная проверка:**
Откройте файл `Employee.cpp`, строка 9:
```cpp
if (name.empty()) {
    throw EmployeeException("Имя сотрудника не может быть пустым!");
}
```

**✅ Критерий успеха:**
- Класс `EmployeeException` определен и наследуется от `std::runtime_error`
- Исключение этого типа генерируется в коде
- Исключение этого типа обрабатывается в блоке `catch (const EmployeeException& e)`

---

## 📊 СВОДНАЯ ТАБЛИЦА ПРОВЕРКИ

| № | Требование | Где проверить | Критерий успеха |
|---|------------|---------------|-----------------|
| 1 | 5+ типов исключений | Меню 10 → Демонстрация 5 | Видно 5 разных типов |
| 2 | Перенаправление | Меню 10 → Демонстрация 4 | Видно "Перехвачено" и "Перенаправление" |
| 3 | Исключение с new | Меню 10 → Демонстрация 3 | Видно `bad_alloc` |
| 4 | Исключение в конструкторе | Меню 10 → Демонстрация 1 | Деструктор НЕ вызывается при исключении |
| 5 | Собственная функция завершения | `main.cpp` строка 239 | `set_terminate(customTerminate)` |
| 6 | Пользовательский класс | `Employee.h` + Меню 10 → Демонстрация 2 | Класс определен и используется |

---

## 🎯 БЫСТРАЯ ПРОВЕРКА (5 минут)

**Минимальный набор действий для проверки всех требований:**

1. **Запустите программу**
2. **Выберите пункт меню `10. Демонстрация исключений`**
3. **Прокрутите весь вывод и проверьте:**
   - ✅ Демонстрация 1: Исключение в конструкторе (требование 4)
   - ✅ Демонстрация 2: Пользовательский класс исключения (требование 6)
   - ✅ Демонстрация 3: Исключение с оператором new (требование 3)
   - ✅ Демонстрация 4: Перенаправление исключений (требование 2)
   - ✅ Демонстрация 5: 5+ типов исключений (требование 1)

4. **Проверьте код:**
   - Откройте `main.cpp`, строка 239: `std::set_terminate(customTerminate);` (требование 5)
   - Откройте `Employee.h`, строки 8-11: класс `EmployeeException` (требование 6)

---

## 🔬 ДОПОЛНИТЕЛЬНЫЕ ПРОВЕРКИ

### Проверка через пользовательский ввод

**Требование 6 (пользовательский класс) через меню:**
1. Меню `3` → Пустое имя → Должно быть `EmployeeException`
2. Меню `4` → Пустое имя → Должно быть `EmployeeException`

**Требование 1 (5+ типов) через меню:**
1. Меню `3` → Отрицательный опыт → `invalid_argument`
2. Меню `3` → Отрицательная зарплата → `invalid_argument`
3. Меню `6` → Отрицательное количество → `invalid_argument`
4. Меню `6` → Недостаточно персонала → `runtime_error`
5. Меню `3` → Дубликат имени → `runtime_error`

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

- [ ] Требование 1: Видны минимум 5 типов исключений в демонстрации 5
- [ ] Требование 2: Видно перенаправление исключения в демонстрации 4
- [ ] Требование 3: Видно исключение `bad_alloc` в демонстрации 3
- [ ] Требование 4: При исключении в конструкторе деструктор НЕ вызывается (демонстрация 1)
- [ ] Требование 5: В коде есть `std::set_terminate(customTerminate)`
- [ ] Требование 6: Класс `EmployeeException` определен в `Employee.h` и используется в коде

**Если все пункты отмечены ✅ - все требования выполнены!**

